# CL-D5-RESEARCH-CONSUMER-20260912：外部研究消费验收交接

状态：本仓样例已经验收；外部回迁、外部进程运行和实盘接入尚未执行。此记录不是已自动派发的任务。D5不缺数据，计算已在会话完成；这里是下一实际外部验收的最小清单。

## 本地需要接收什么

从本次已合并PR取得 research/state_degree_consumer_d5/ 中协议、五个Python文件、结果、来源身份、执行与独立复核回执及本页；连同既有治理文件。完整D5证据包包含原D2/D4输入ZIP、D5事件视图、边界查询与日志；大产物留本地，不需要向云端搬新行情。

先核对 SOURCE_IDENTITY.json、输入/输出manifest，按 REPRODUCE.md 跑40项测试、独立232704条事件与930816次查询复核，以及零/两秒合成延迟样例。无需拟合V19/D3/D4，也不以重放刷新的样本外证据。

## 真正外部消费的最低要求

在本地研究环境的独立消费进程中，用明确的 as_of 与 received_at 读取相同冻结事件，核对状态、确认性质、I/V与lag/delta来源、不可用/失效原因、时区和版本。比较已封存样例与独立验证结果；不要把本会话输出复制成“本地测试通过”。不得连接生产registry、下单、策略router或仓位账户。

严禁拿CLOSE覆盖E15、把E15增强值填到CLOSE、把UNAVAILABLE补NORMAL、把概率缺失补0/1，或用训练期全样本分位键做当时已经部署的门控。新增接收日志必须真实产生并声明来源；历史理想时间不改称实测。

## 本地回传一份小回执

注明实际机器/环境、代码commit与文件SHA、输入包SHA、命令、退出码、测试数、比对差异与结果摘要哈希，区分“已回迁”“测试已执行”“外部研究消费已验证”“生产未授权”。返回所有失败和未执行项；大文件不必回传。

云端收到回执后只复核收到的范围，不能冒称云端独立重跑全量本地环境。若无真实接入需求，不必新建模型课题，保持本仓冻结维护即可。D6/V20未启动；production_authority=false。
