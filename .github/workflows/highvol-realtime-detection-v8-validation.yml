name: highvol-realtime-detection-v8-validation
on:
  push:
    branches:
      - research/highvol-realtime-detection-v8-validation-20260910
    paths:
      - .github/workflows/highvol-realtime-detection-v8-validation.yml
      - research/highvol_realtime_detection_v8_validation/**
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout frozen detector and Validation-only measured inputs
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            research/highvol_realtime_detection_v8
            research/highvol_realtime_detection_v8_validation
            docs/governance/DATA_USAGE_POLICY_V2.md
            docs/governance/data_usage_declaration.json
            docs/governance/blackbox_query_ledger.json
            scripts/validate_data_usage_policy.py
            data/market/5m/000688.SH/2020.parquet
            data/market/5m/000688.SH/2021.parquet
            data/market/5m/000688.SH/2022.parquet
            data/market/5m/000688.SH/2023.parquet
            data/market/5m/000688.SH/2024.parquet
            data/market/5m/000688.SH/2025.parquet
            data/market/5m/000852.SH/2020.parquet
            data/market/5m/000852.SH/2021.parquet
            data/market/5m/000852.SH/2022.parquet
            data/market/5m/000852.SH/2023.parquet
            data/market/5m/000852.SH/2024.parquet
            data/market/5m/000852.SH/2025.parquet
            data/cross_index_risk_gate_3s_v1/000688.SH_2024.parquet
            data/cross_index_risk_gate_3s_v1/000688.SH_2025.parquet
            data/cross_index_risk_gate_3s_v1/000852.SH_2024.parquet
            data/cross_index_risk_gate_3s_v1/000852.SH_2025.parquet
          sparse-checkout-cone-mode: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install fixed dependencies
        run: python -m pip install --disable-pip-version-check numpy pandas pyarrow pytest
      - name: Validate three-pool governance
        run: python scripts/validate_data_usage_policy.py --repo-root .
      - name: Guard physical Validation subset and BlackBox exclusion
        run: |
          python - <<'PY'
          from pathlib import Path
          for s in ['000688.SH','000852.SH']:
              got=sorted(p.name for p in (Path('data/market/5m')/s).glob('*.parquet'))
              assert got==[f'{y}.parquet' for y in range(2020,2026)],(s,got)
          got=sorted(p.name for p in Path('data/cross_index_risk_gate_3s_v1').glob('*.parquet'))
          exp=sorted([f'{s}_{y}.parquet' for s in ['000688.SH','000852.SH'] for y in [2024,2025]])
          assert got==exp,(got,exp)
          assert not list(Path('data').rglob('2026.parquet'))
          assert not any('blackbox' in str(p).lower() for p in Path('data').rglob('*'))
          print('V8 Validation physical boundary: OK')
          PY
      - name: Test frozen Validation contract
        run: python -m pytest -q research/highvol_realtime_detection_v8_validation/test_validation.py
      - name: Run frozen V8 3s Validation subset
        run: python research/highvol_realtime_detection_v8_validation/run_validation.py --repo-root . --out artifacts/highvol_realtime_detection_v8_validation
      - name: Guard research-only authority
        run: |
          python - <<'PY'
          import json
          s=json.load(open('artifacts/highvol_realtime_detection_v8_validation/summary.json'))
          assert s['validation_subset_years']==[2024,2025]
          assert s['validation_complete_through_2026_08_21'] is False
          assert s['validation_2026_queried'] is False
          assert s['threshold_search_performed'] is False
          assert s['state_thresholds_unchanged'] is True
          assert s['pnl_computed'] is False
          assert s['trading_rule_created'] is False
          assert s['blackbox_queried'] is False
          assert s['production_authority'] is False
          assert s['complete_validation_pass'] is False
          print('available 2024-2025 Validation subset pass:',s['available_validation_subset_pass'])
          print(json.dumps(s['acceptance'],sort_keys=True))
          PY
      - name: Seal output manifest
        run: |
          python - <<'PY'
          from pathlib import Path
          import hashlib,json
          root=Path('artifacts/highvol_realtime_detection_v8_validation'); files=[]
          for p in sorted(root.iterdir()):
              if p.is_file():
                  b=p.read_bytes(); files.append({'name':p.name,'bytes':len(b),'sha256':hashlib.sha256(b).hexdigest()})
          (root/'output_manifest.json').write_text(json.dumps({'schema':'highvol_realtime_detection_v8_validation_manifest','files':files},indent=2)+'\n')
          PY
      - name: Upload V8 Validation evidence
        uses: actions/upload-artifact@v4
        with:
          name: highvol-realtime-detection-v8-validation-${{ github.run_id }}
          path: artifacts/highvol_realtime_detection_v8_validation
          retention-days: 30
