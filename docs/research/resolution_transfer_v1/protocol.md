# 科创50秒级分辨率与中证1000半日低通迁移：结果前合同

用户2026-09-07授权两件事：寻找科创50指数或ETF真实秒级数据，检查15s/30s等观测尺度下路径效率方差；将本次半日低通迁移至中证1000指数1m，报告年化。保持两项问题分开，不用分辨率结果替代交易回测，不搜索新策略参数。

## 来源和权限

科创50指数3秒v4产品物理存在，但当前 `fixed_version_market_index_transactions.v1` 消费合同仍限2014-10-17—2020-12-31；本轮不绕过、不改写其服务授权、不直读该湖文件。按照用户明确允许ETF替代，选用已验收的 `fund_l2_quote_change_star50_etf_20240102_20251231_v1.1_20260906`，消费合同 `factorlab_star50_etf_spot_l2_20240102_20251231.v1`。每次会话先检查当前精确grant与manifest，逐日验证文件摘要。588000.SSE为主要观测，588080.SSE为同类复核，2024和2025分别报告；它们共享标的，不是两个独立市场。11月18/19日2024源缺口如实排除。

只计算现有真实源观测在3/15/30/60/300秒网格的价格测量，不物化OHLC或宣称DataHub新增交易bar。primary价格为BBO中间价，另以真实last_price复核。网格点只允许选≤该点、年龄≤3秒、有效区间覆盖该点的已观测状态；无支持置缺失，不插值、不跨午休/隔夜填充；同一时间的零有效区间记录不当有效状态。旧报价可以真实不变，需报告重复价比例，不能把不变等同更低市场风险。

中证1000使用DataHub固定交付 `factorlab_unified_index_kline_v3_20260824/1m_official.parquet`，绑定manifest摘要；过滤symbol=000852.SH、2020—2025，2020仅预热。与本次科创50V2相同2021—2025重复历史评估；该新任务由用户明确授权跨标的固定迁移，不使用旧Layer1窄范围3秒adapter。2026在读取谓词中物理排除。

## 分辨率测量

`ER=|x(t)-x(t-H)|/sum|dx|`，x为log价格。主H=30交易分钟；敏感性H=15/60分钟。固定实际时长，频率对应不同根数。ER动态方差在固定60分钟W内、统一5分钟决策点计算；同段对照的网格嵌套且两端相同。只统计连续上午/下午时段内部，缺失窗口拒绝。零路程的ER未定义，不填0。每个H还报告每H分钟一次的非重叠ER，避免把重复滚动窗当独立证据。

同时报告：ER均值、原始Var(ER)、`n*Var(ER)`（n=H/Δ）、Var/mean²、方差动态、固定5分钟ER变化、零收益比例、单个增量占总绝对变化比例、粗/细观察到的路程比、实际价格样本年龄与覆盖。理由：在独立随机游走基准下，ER均值约按1/√n缩小，原始方差可因分辨率变细而机械下降；低方差也可能只是长期低效率或陈旧价，不能推出平稳性或可交易性。

作为完成窗后验诊断，可把30min窗口中单个3s位移贡献≥50%记为jump_dominated，ER≥0.6且单个贡献<20%记为directional_resolved_candidate，其余mixed；仅作形态标记，不用作实时路由或事前盈利标签。3秒也不是连续路径真值，亚3秒结构仍不可识别。不会声称看完整个窗口的属性可以提前抓取该窗口。

不预定哪个频率必须获胜。比较同日同会话同结束时刻共同有效窗口，给每年的方向和覆盖变化；不按这次结果选择交易频率。两个ETF/last与mid/多个H属于依赖复核，而非独立显著性票数。

## 固定策略迁移

将原V2整案原样用于000852.SH：1m、120min一阶低通、h=1..5、entry_alpha=.001、exit_alpha=.05、weight_power=2、volatility_window=480、entry_confirm=1、cooldown=5。本任务说到“带通”后又明确“用低通0.5”，因此以当前半日低通为迁移主体，不额外选择带通上下边界；V1只作固定控制，不是候选排名池。

使用已验收的 `slope_union_v2.features/targets/account`，raw下一开盘、固定入场份额、年度隔离后顺次复投、0/2/4/7bp费用代理，双向指数模拟。先检验时钟与V1原实现等价；2021→2025每年独立执行，主控阅读结果并记录再开下一年。V2始终不变，不优化、失败也不修改。输出年化、日Sharpe、分钟MDD、平均每笔净简单收益、笔数与年度分布；没有实际IM/MO/ETF成交或新鲜OOS权威。

完整审计分别绑定分辨率输入/测量与迁移输入/账户；测试覆盖零路程、缺失拒绝、固定时长、纯趋势/随机游走/跳变反例、缩放不变、下一开盘和独立现金份额对账。保留所有未完成与失败项，不把权限或缺口当作科学失败。
