# 样本损失与冲击前状态：执行结果

日期：2026-09-07。研究分支：`research/measurement-support-audit-20260907`。
基线：`424a6b5a931cf610aa731125aa2081cdf3accbbb`。
执行提交：`83e63394f6418a48bbdca92f8bdea4fe01124077`。
Actions：`34100182102`，首次运行成功。

## 当前是否需要用户协助

**暂不需要新增上传或授权。现有合格时段能够继续做研究。**
确实存在一个历史数据质量缺口：较早秒级源不能支持连续秒内路径；若将来必须恢复原2021—2022完整训练方案，才需要上游更完整的历史源。重新上传同一批文件不能解决这个缺口。它不阻断使用现有完整片段继续做结构检验。

## 1. 已查清样本为什么减少

在本次2021—2025范围内，2021年两指数源观测集中于每分钟0至33秒，偶见36秒；39至59秒没有观测。科创50、中证1000分别出现56,284、56,275个27秒间隔。这是已交付归档的采样结构，不是仓库访问权限问题；尚未识别供应商为什么采用该结构。

在冻结的15秒测量规则下，2021-01-04至2022-05-13，两指数最长只能形成两个连续有效15秒收益，不能组成完整5分钟或15分钟回看。2021年全部基线可决定样本首先被5分钟完整性条件排除；不是跨尺度分类器额外筛掉的结果。

**本次范围内，首次出现合格秒级特征决定点的日期为2022-05-16。** 这不表示该日之后每一个窗口都完整，也不是指数或产品的官方发布日期。后续仍逐窗口检查端点年龄和源缺口。

不能靠插值、把缺口当零收益，或降低质量要求来宣称恢复了真实秒内路径。端点收益可以另行测量，但不能据此获知缺口中是否往返。

## 2. 训练正窗口实际只来自多少次事件

按V2不变的事件、标签时距和质量规则，从原始分钟及秒级出口重新建立了逐事件与训练窗口的对应：

| 指数 | V2拟合期正窗口 | 对应的独立首事件 |
|---|---:|---:|
| 科创50 | 58 | **6** |
| 中证1000 | 14 | **1** |

科创5058个窗口来自2022-06-15、07-06、10-12、10-24、11-09、12-13这六个事件；中证1000的14个窗口全部来自2022-07-06的同一事件。事件识别是按冻结规则计数，不将同一事件的多个前瞻窗口视为独立样本。

这个结果补齐了V2此前未精确重放的训练事件覆盖数。它说明监督训练的独立正例极少，不证明跨尺度信息永远无用，也不能被20个正窗口的旧最低工程门槛替代。

## 3. 已按真正事前状态拆分事件

保留全部104个历史首事件锚点。在每个事件之前最新的允许决定点，检查其过去5分钟路径；决定点取`min(事件收盘分钟-2,105)`，始终至少早于事件分钟最早可能起点一分钟。

描述口径先固定：过去5分钟绝对净位移达到30bp为“已经明显走动”；净位移不到30bp但价格区间达到30bp为“已经大幅往返”；区间不到30bp为“低观测振幅”；无法完整观察则为Unknown。它只是事后事件账本中的事前状态分类，不是新增训练标签或经过验证的门控。

2024—2025的77个事件，其严格秒级前缀均可计算：

| 事前5分钟状态 | 科创50 | 中证1000 |
|---|---:|---:|
| 已有至少30bp净位移 | 14 | 3 |
| 区间至少30bp、但净位移不足30bp | 12 | 0 |
| 区间不足30bp | 26 | 22 |
| 合计 | 52 | 25 |

因此，77个事件中有29个在合格预警时刻之前就已有较大移动或往返。余下48个只是按此固定30bp标准属于低振幅；不能称为已证明平稳、无风险或可预测的事件。这些数字的观察时点比上一轮“冲击分钟开始前5分钟净位移”更早，定义不同，不覆盖旧统计。

## 4. 执行和独立复核

没有重新训练模型，没有调整V2标签、阈值或缺口准入，也没有打开2026。原始二十个授权分区全部由冻结验证器核查；24项新增工程测试在本会话和Actions均通过。

十个指数—年份的V2主支持计数逐项完全复现。逐事件支持窗口总数与正标签总数一致；源间隔计数、每分钟秒位置计数、样本排除分解均能对账。

本会话下载了52,567字节的产物ZIP，SHA256为`f6605358c3330b3e7634ab2f35f965dc1aa4522a65b42406d08f86b629528e49`，与GitHub产物摘要一致。核对13个清单输出文件哈希、104个唯一事件；另从之前冻结的V2导出路径独立重算100条事前前缀，最大差2.45e-12bp。另4条超出旧路径导出边界，不冒称重算通过。

本会话复核对象是Actions输出及已导出的源路径子集，不是在本会话独立读取全部原始Parquet再训练。独立复核首次因参考CSV保留的旧行索引不同失败；仅重置比较索引后通过，未修改市场算法或结果。脚本、限制和回执随交付保留。

## 5. 下一步与数据需求边界

继续用满足质量要求的现有时段，先检验跨尺度异常与后续风险的条件关系，并把低振幅后突发、已推进行情和大幅往返分开；不再把58/14个重叠窗口当成足够的独立训练事件。

新的模型或事件定义应另行在看新结果之前登记。优先考虑不靠这6/1次冲击拟合复杂监督分类器的测量及条件结构检验，保留原V2参照；历史仍为consumed development，不称新的独立OOS。

**非阻断的上游补全需求**：只有恢复早期连续路径成为必要条件时，才请DataHub核查两指数2021-01-04—2022-05-13是否另有连续秒级父源。最小核查可先对照2021-01-04、2022-05-13及2022-05-16三个源日，提供symbol、原时间、row_index、price、父源hash和采样语义；不需要ETF、期权、盘口或2026，不要求重新上传整库。本轮没有自动联系或派发本地模型。

科学状态：**数据损失原因和实际独立训练样本数已查清；事件的事前状态已拆分；没有新增风险预测成功结论。**
